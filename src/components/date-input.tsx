type DateInputProps = {
  label: string;
  value: string;
  onChange: (value: string) => void;
  theme: {
    text: string;
    textSecondary: string;
    background: string;
    backgroundSelected: string;
  };
};

export function DateInput(_props: DateInputProps) {
  return null;
}
